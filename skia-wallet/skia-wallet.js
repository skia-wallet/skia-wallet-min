function execute(contractAddress, method, args, callBackUrl)
{
	if (typeof(window) == 'object')
	{
		const contractAddressEncoded = 
		window.location.href = encodeURI('https://skiawallet-min.vercel.app/transact?')
